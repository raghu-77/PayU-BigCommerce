export default function ErrorPage() {
    return (
      <div>
        <h1>Session Expired</h1>
        <p>Your session has expired due to inactivity or reaching the maximum session duration.</p>
      </div>
    );
  }
  